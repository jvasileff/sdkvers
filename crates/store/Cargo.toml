[package]
name = "store"
version = "0.1.0"
edition = "2021"

[dependencies]
types = { path = "../types" }
thiserror = "2"
tar = "0.4"
flate2 = "1"
zip = { version = "2", default-features = false, features = ["deflate"] }
md5 = "0.7"
sha2 = "0.10"

[dev-dependencies]
tempfile = "3"
