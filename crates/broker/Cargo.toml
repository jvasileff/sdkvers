[package]
name = "broker"
version = "0.1.0"
edition = "2021"

[dependencies]
types = { path = "../types" }
thiserror = "2"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "rustls-tls"] }
md5 = "0.7"
